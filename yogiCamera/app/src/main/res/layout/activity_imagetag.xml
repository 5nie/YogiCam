<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:background="#90000000"
    android:layout_height="match_parent">
    <ImageView
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:src="@drawable/test_image_one"
        android:id="@+id/capture_image"
        android:layout_marginTop="31dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />
    <ImageView
        android:layout_width="100dp"
        android:layout_height="50dp"
        android:src="@drawable/activity_imagetag_location_icon"
        android:layout_below="@+id/capture_image"
        android:layout_alignStart="@+id/capture_image"
        android:id="@+id/location_view" />
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="#Taging_Your_Location"
        android:textColor="#000000"
        android:background="@null"
        android:layout_alignStart="@id/location_view"
        android:id="@+id/editText"
        android:layout_below="@id/location_view"
        android:layout_marginTop="16dp" />

</RelativeLayout>